spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/seniar_chat
    username: seniar
    password: seniar

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.seniar: DEBUG
    org.springframework.ai: DEBUG
    org.hibernate.SQL: DEBUG
